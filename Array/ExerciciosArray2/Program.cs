using System;

namespace ExerciciosArray2
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Executando lista 2 de exercícios de Array");

            //1) Crie 2 vetores de INTEIROS com 5 posições, some os valores do mesmo indície dos 2 vetores (ex: array[0] == 3 e array2[0] == 4 ; entao; 3 + 4 = 7) e armazene a conta em um terceiro vetor.

            //2) Crie um array para armazenar os 5 produtos de uma padaria, por ser uma padaria muito concorrida, cada cliente pode comprar até 10 //produtos por compra(outro array) o cliente pode tirar produtos  caso nao tenha dinheiro para comprar, os preços dos produtos é : cuca /– /10 , pão - 1, pastel – 5, coxinha – 4 e bolo – 35, além disso, a qualquer momento o usuario pode solicitar o valor atual da compra

            //3) Crie 3 vetores de 10 posições (pode pedir ao usuario ou criar um Random{entre 0 e 30} para facilitar o preenchimento) confira qual o //maior número entre os 3 vetores no mesmo índice (ex: a[1] == 2 , a2[1] == 4, a3[1] 3 ; nesse caso a2[1] é o maior) para todo o vetor, //após isso, some apenas os maiores números de cada índice

            //4) Crie a criptografia de Cesar, a criptografia de Cesar funciona da seguinte forma, a letra colocada é substituida por uma letra que /esta/ 3 passos a frente (ex: a → d, g → j) para esse exercício desconsidere (Z,W,Y,Ç), a criptografia deve ser feita em uma senha que terá no max 10 caracteres (deve-se criar um //array para isso, e mudar a posição da letra no array) ps: pode ser feita usando vetores e pode ser feita usando um if apenas

            //5) Criea descriptografia de Cesar, a descriptografia de Cesar funciona da seguinte forma, a letra colocada é substituida por uma letra que esta 3 passos para trás (ex: a → d, g → j) para esse exercício desconsidere (Z,W,Y,Ç), a descriptografia deve ser feita em uma senha que terá no max 10 caracteres (deve-se criar um array para isso, e mudar a posição da letra no array) ps: pode ser feita usando vetores e pode ser feita usando um if apenas

        }
    }
}
